import { asyncScheduler } from 'rxjs/dist/esm/internal/scheduler/async';
import { throttle } from 'rxjs/dist/esm/internal/operators/throttle';
import { timer } from 'rxjs/dist/esm/internal/observable/timer';
export function throttleTime(duration, scheduler = asyncScheduler, config) {
    const duration$ = timer(duration, scheduler);
    return throttle(() => duration$, config);
}
//# sourceMappingURL=throttleTime.js.map