import { raceInit } from 'rxjs/dist/esm/internal/observable/race';
import { operate } from 'rxjs/dist/esm/internal/util/lift';
import { identity } from 'rxjs/dist/esm/internal/util/identity';
export function raceWith(...otherSources) {
    return !otherSources.length
        ? identity
        : operate((source, subscriber) => {
            raceInit([source, ...otherSources])(subscriber);
        });
}
//# sourceMappingURL=raceWith.js.map